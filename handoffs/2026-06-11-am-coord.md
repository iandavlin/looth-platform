# SESSION-HANDOFF — bespoke-cutover (Hub coordinator)

**Session:** 2026-06-11. **Successor: read this, then the architecture brain —
`docs/bespoke-cutover-charter.md` + `docs/hub-architecture-audit.md`. Prior live
state is `handoffs/2026-06-10-fable.md` (still accurate except where noted below).**

You are `ubuntu`, full sudo, ON the dev box (curl ifconfig.me → 50.19.198.38 →
act locally, never SSH). The fork IS what dev serves: branch `bespoke-cutover`
at `~/worktrees/bespoke-cutover/`; `/srv/bb-mirror` symlinks into it, so edits
to `bb-mirror/**` (incl. forums.css/js, *.php) are **live on save**. Commit small
by pathspec; **NEVER push** (Ian reviews, git-tsar pushes). Don't work in
`/home/ubuntu/projects` (git-tsar auto-sweeps it — it already swept this session's
`_feed.php` edit into commit `49dd02b` under an unrelated label; expected, don't
relabel). Gate cookie `loothdev_auth` value is in the nginx conf; CDP via the
`chrome-dev-login` skill (claude_admin is now **user ID 1912** after a DB reload —
1904 is dead).

---

## 🚨 CRITICAL FINDING — the overlay fork mirror is ~18 versions STALE vs live

The handoff model "edit `hub-overlay-flag/` copies, then `cp` to `/var/www/dev/`"
**is broken and will roll back live work if followed blindly.**

- `/var/www/dev/hub-polish.js` (LIVE) is at **v174**. The fork mirror
  `hub-overlay-flag/hub-polish.js` says **v156**. ~652 lines of newer work live
  only in `/var/www/dev/` and were never mirrored back (Buck's Like-icon
  thumbs-up, `ensureReplyNameWrapCss` long-name fix, ultrawide column caps at
  1780/2872/3414px, more).
- Live `pwa.js` is ahead too: app-settings v31 (fork v29), bottom-nav v21 (v18),
  shop-bubble v18 (v8), directory-mobile v11, sponsor-sheet v2, **a whole new
  `messenger-sheet.js?v=1` loader block** — none in the fork pwa.js.
- **Implication:** `/var/www/dev/*.js` is the real source of truth for the
  OVERLAY, not the fork. Someone (Buck?) has been hot-deploying to `/var/www/dev`
  without mirroring. Until reconciled, treat the LIVE file as base for any overlay
  edit, and **never `cp` the fork overlay over live.**
- **NOT broken:** `forums.css` / `forums.js` / the `*.php` — those ARE symlinked
  (fork == live), zero drift. Only the non-symlinked `/var/www/dev/*.js` overlay
  drifted.
- **Reconcile job (own it or route to Buck-coord):** pull `/var/www/dev/*.js`
  (hub-polish v174, pwa, app-settings, bottom-nav, messenger-sheet, etc.) back
  into `hub-overlay-flag/`, commit as "reconcile fork mirror to deployed v174",
  THEN resume overlay work from there.

---

## What landed this session

1. **✅ `/img.php` on-the-fly cover resizer — DONE, live, verified.**
   - Feed covers served full-res originals (3024px phone photos, ~9 MB/page).
     New `bb-mirror/web/img.php` transcodes `/wp-content/uploads/` covers to
     width-capped WebP with a local disk cache (`/var/cache/lg-img`, owned
     looth-dev). `lg_cover_src()` in `_feed.php` routes covers through it
     (cards `w=800`, forum banner `w=1600`; external URLs pass through).
   - **Runs on the looth-dev FPM pool**, NOT bb-mirror: the R2 uploads mount
     (`/mnt/loothgroup-uploads-dev`, symlinked from `…/wp-content/uploads`) is
     only readable by members of the `loothdevs` group (www-data, looth-dev);
     bb-mirror + archive-poc are NOT in it and get permission-denied.
   - **nginx:** added `location = /img.php` to
     `/etc/nginx/snippets/strangler-bb-mirror.conf` → looth-dev sock,
     `SCRIPT_FILENAME /srv/bb-mirror/web/img.php`, dev-cookie-gated. Plain
     `fastcgi.conf` (not `fastcgi-php.conf` — the latter's try_files checks the
     WP docroot for /img.php and 404s). This is a **sysadmin `/etc` change — it
     does NOT ride a git pull**; add to the cut playbook (charter §2 Track 2).
   - Verified on dev: 592 KB JPEG → 75 KB WebP @800w (8:1); all 11 feed covers
     emit `/img.php`; traversal→400, missing→404, no-cookie→403, cache populates.
   - **Commits:** `f26ac06` (img.php). The `_feed.php` helper+wiring was swept
     into `49dd02b` by the git-tsar. Nothing pushed.

2. **🟡 Desktop "drop the lg-feed-booting blank" (the flash fix) — STARTED,
   INCOMPLETE, currently harmless. See next section — the work as scoped in the
   old handoff was wrong; it's bigger.**

---

## 🟡 Item: desktop flash fix — corrected scope + exact next steps

The old handoff said "C3 done → just drop the lg-feed-booting blank on desktop."
That is **not** droppable yet. The blank exists because the desktop card is
**chrome-less on first paint** — the entire card shell (bg/border/16px/shadow),
Buck's "React" pill, the longer search box, the hidden T/C/Cpt toggles, AND a
stack of theme-color rules are all injected by `hub-polish.js → ensureDesktopCss`
AFTER load. Drop the mask today and all of that flashes in. So the real
prerequisite is folding `ensureDesktopCss` into `forums.css @media(min-width:641)`
value-identical (the audit's "Days 3–5" work), THEN drop the mask.

**Ian's decisions (this session):** grind it now, CDP-verify; **card radius stays
16px** (current look — zero visual change).

**What I did (live but harmless):**
- Added a `DESKTOP CARD CHROME + THEME FOLD (≥641)` block to `bb-mirror/web/forums.css`
  (search that string). It's LIVE (symlink). It is currently **redundant** — live
  v174 `hub-polish.js` still injects the same rules, so no visible change, no
  regression. **But it is INCOMPLETE** — I derived it from the STALE fork (v156);
  live v174 also has the merged masonry/display props on `.feed-card` and three
  **ultrawide column caps** (`@media min-width:1780/2872/3414`, Buck 2026-06-11)
  that my fold does NOT include.
- Added an early-`return` to `hub-overlay-flag/hub-polish.js ensureDesktopCss`
  (fork only — NOT deployed, fork isn't served, so no effect).

**To finish it RIGHT (do NOT use the stale fork):**
1. Re-derive the fold from **LIVE** `/var/www/dev/hub-polish.js` v174
   `ensureDesktopCss` (lines ~3167–3296). My forums.css block covers most of it;
   ADD the ultrawide caps (1780/2872/3414) it's missing. Confirm radius 16px.
2. CDP-verify (desktop 1280 + an ultrawide width, default/light/dark themes) that
   `forums.css` alone (overlay injection disabled) renders **identical** to the
   baseline at `/var/www/dev/mockups/hub-before.png` (captured this session).
3. Retire `ensureDesktopCss` in the **live** v174 file (early-return), bump
   `pwa.js` hub-polish `?v=174→175`, deploy to `/var/www/dev` (cp preserves
   buck's ownership), AND mirror both into `hub-overlay-flag/` (this is also the
   reconcile moment — capture all of v174 first).
4. Only then drop the mask: in the nginx `</head>` sub_filter
   (`/etc/nginx/sites-available/dev.loothgroup.com.conf:34`), gate the
   `lg-feed-booting` class-add to `≤640` (mobile keeps it — mobile still has the
   C9 JS-reshape debt). `nginx -t && reload`. CDP-verify desktop: feed visible
   immediately, no blank, no flash. Mobile unchanged.
- **Fallback if you don't want to grind it now:** revert my forums.css fold block
  for a clean tree — it's self-contained and clearly commented.

---

## Open queue (corrected order)

1. **Finish the desktop flash fix** (above) — re-derive from live v174, verify,
   retire ensureDesktopCss, drop the mask. Reconcile the overlay fork as part of it.
2. **Modal → mobile port** (Ian liked the desktop discussion modal): port §4e
   full-screen ≤640, retire the lrs replies sheet (~200 overlay lines). Soft edge:
   after posting a reply the modal doesn't live-refresh (close/reopen does).
3. Profile dark-mode pass (`docs/handoff-profile-darkmode.md` — Ian may spawn separate).
4. lg-layout-v2 dark overhaul (charter post-launch; design call pending).
5. Launch wiring Track 2 (charter §2) — **add the `/img.php` route + `/var/cache/lg-img`
   to the live nginx/cut playbook.**

## Loose ends / hazards
- Working tree also has uncommitted `bb-mirror/api/v0/reply.php`, `forums.js`,
  `forums/_reply-render.php` that **I did not author or touch** — pre-existing
  in-flight work. Don't bundle them with overlay/img work; check with Ian before
  committing them.
- Overlay deploys regress live if sourced from the fork — see CRITICAL FINDING.
- `/img.php` `/etc` route + cache dir don't ride a git pull (cut playbook).

## Current versions (LIVE, from /var/www/dev/pwa.js)
hub-polish **v174** · app-settings v31 · bottom-nav v21 · shop-bubble v18 ·
mobile-hub v3 · app-mobile-fixes v28 · messenger-sheet v1 (new). Fork mirror is
stale at hub-polish v156 — reconcile before deploying any overlay.
