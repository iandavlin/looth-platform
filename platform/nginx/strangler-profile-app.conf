# ===== profile-app — strangler for member profile 2.0 =====
# Owns: /profile/edit, /u/<slug>, /u/<slug>/edit, /p/<slug>,
#       /profile-api/v0/*, /directory/members, /members/* hijack redirects.
# Source: /srv/profile-app/
# FPM pool: php8.3-fpm-profile-app.sock
# Authoritative scope: /home/ubuntu/projects/docs/STRANGLER-COORDINATION.md


# /profile/edit (and /profile/edit/) → SSR editor (web/edit.php)
location ^~ /profile/edit {
    if ($loothdev_is_authorized != 1) { return 403; }
    alias /srv/profile-app/web/;
    rewrite ^/profile/edit/?$ /profile/edit/edit.php last;

    location = /profile/edit/edit.php {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/edit.php;
    }

    # Static assets under /profile/edit/<file>.{css,js,…}
    location ~ ^/profile/edit/([\w\-.]+\.(css|js|svg|png|jpg))$ {
        alias /srv/profile-app/web/$1;
    }

    location ~ \.php$ { return 403; }
}

# /u/<slug> public read-only view + /u/<slug>/edit alias
# NB: include fastcgi.conf (NOT snippets/fastcgi-php.conf) so we skip
# the try_files-against-document_root check that 404s on URIs without .php.
location ~ "^/u/([\w\-]+)/edit/?$" {
    if ($loothdev_is_authorized != 1) { return 403; }
    include fastcgi.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
    fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/u-edit.php;
    fastcgi_param QUERY_STRING    slug=$1&$args;
}
location ~ "^/u/([\w\-]+)/?$" {
    if ($loothdev_is_authorized != 1) { return 403; }
    include fastcgi.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
    fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/u.php;
    fastcgi_param QUERY_STRING    slug=$1&$args;
}

location ~ "^/p/([\w\-]+)/?$" {
    if ($loothdev_is_authorized != 1) { return 403; }
    include fastcgi.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
    fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/p.php;
    fastcgi_param QUERY_STRING    slug=$1&$args;
}

# BB profile hijack (dev only): the canonical profile page is now /u/<slug>.
# /members/<nicename>/                  -> 302 /u/<nicename>
# /members/<nicename>/profile/          -> 302 /u/<nicename>
# /members/<nicename>/profile/edit/     -> 302 /profile/edit
# Other BB sub-routes (forums, connections, photos, ...) still hit WP.
location ~ "^/members/([\w\-]+)/profile/edit/?$" {
    return 302 /profile/edit;
}
location ~ "^/members/([\w\-]+)(/profile)?/?$" {
    return 302 /u/$1;
}

# Internal service channel (poller <-> profile-app, shared-secret auth at PHP layer).
# Must be ABOVE the main /profile-api/v0/ block so the more-specific ^~ wins.
    # ----- avatar image serve (profile-app single-source media store) -----
    location ^~ /profile-media/ {
        if ($loothdev_is_authorized != 1) { return 403; }
        alias /srv/profile-app-media/;
        try_files $uri =404;
        expires 30d;
        add_header Cache-Control "public";
    }

location ^~ /profile-api/v0/internal/ {
    allow 127.0.0.1;
    allow ::1;
    deny all;
    # No cookie gate — internal only, X-LG-Internal-Auth verified in PHP
    # via hash_equals() against /etc/lg-internal-secret.
    # Route everything under /internal/ to the single PHP handler via explicit
    # SCRIPT_FILENAME — keeps things simple, no try_files = no document-root games.
    location ~ "^/profile-api/v0/internal/purge-whoami/?$" {
        allow 127.0.0.1;
        allow ::1;
        deny all;
        include fastcgi.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME /srv/profile-app/api/v0/internal-purge-whoami.php;
    }
    location ~ "^/profile-api/v0/internal/erase-user/?$" {
        allow 127.0.0.1;
        allow ::1;
        deny all;
        include fastcgi.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME /srv/profile-app/api/v0/internal-erase-user.php;
    }
    return 404;
}

location ^~ /profile-api/v0/ {
    alias /srv/profile-app/api/v0/;

    rewrite "^/profile-api/v0/hooks/user-created/?$"        /profile-api/v0/user-created.php last;
    rewrite "^/profile-api/v0/hooks/email-changed/?$"      /profile-api/v0/internal-email-changed.php last;
    rewrite "^/profile-api/v0/user/([0-9a-fA-F-]{36})/?$"   /profile-api/v0/user.php?uuid=$1 last;
    rewrite "^/profile-api/v0/me/?$"                        /profile-api/v0/me.php             last;
    rewrite "^/profile-api/v0/me/about/?$"                  /profile-api/v0/me-about.php       last;
    rewrite "^/profile-api/v0/me/header/?$"                  /profile-api/v0/me-header.php      last;
    rewrite "^/profile-api/v0/me/location/?$"               /profile-api/v0/me-location.php    last;
    rewrite "^/profile-api/v0/me/location/search/?$"        /profile-api/v0/me-location-search.php last;
    rewrite "^/profile-api/v0/me/socials/?$"                /profile-api/v0/me-socials.php     last;
    rewrite "^/profile-api/v0/me/craft/?$"                  /profile-api/v0/me-craft.php       last;
    rewrite "^/profile-api/v0/me/gallery/?$"                 /profile-api/v0/me-gallery.php     last;
    rewrite "^/profile-api/v0/me/connect/?$"                 /profile-api/v0/me-connect.php     last;
    rewrite "^/profile-api/v0/me/layout/?$"                  /profile-api/v0/me-layout.php      last;
    rewrite "^/profile-api/v0/me/lights/?$"                  /profile-api/v0/me-lights.php      last;
    rewrite "^/profile-api/v0/me/catalog/(skills|services|instruments|music)/?$"  /profile-api/v0/me-catalog.php?block=$1 last;
    rewrite "^/profile-api/v0/me/practice-header/?$"     /profile-api/v0/me-practice-header.php last;
    rewrite "^/profile-api/v0/me/practice-about/?$"      /profile-api/v0/me-practice-about.php last;
    rewrite "^/profile-api/v0/me/practice-layout/?$"     /profile-api/v0/me-practice-layout.php last;
    rewrite "^/profile-api/v0/me/practice-block/?$"      /profile-api/v0/me-practice-block.php last;
    rewrite "^/profile-api/v0/me/practice-services/?$"    /profile-api/v0/me-practice-services.php last;
    rewrite "^/profile-api/v0/me/practice-instruments/?$" /profile-api/v0/me-practice-instruments.php last;
    rewrite "^/profile-api/v0/me/avatar/?$"             /profile-api/v0/me-avatar.php          last;
    rewrite "^/profile-api/v0/me/banner/?$"             /profile-api/v0/me-banner.php          last;
    rewrite "^/profile-api/v0/me/resume/?$"             /profile-api/v0/me-resume.php          last;
    rewrite "^/profile-api/v0/me/discussion-visibility/?$"  /profile-api/v0/me-discussion-visibility.php last;
    rewrite "^/profile-api/v0/me/freeform/?$"           /profile-api/v0/me-freeform.php        last;
    rewrite "^/profile-api/v0/me/dropoffs/?$"           /profile-api/v0/me-dropoffs.php        last;
    rewrite "^/profile-api/v0/me/connections/pending/?$"          /profile-api/v0/me-connections.php?filter=pending last;
    rewrite "^/profile-api/v0/me/connections/?$"                  /profile-api/v0/me-connections.php last;
    rewrite "^/profile-api/v0/connections/?$"                      /profile-api/v0/me-connections.php last;
    rewrite "^/profile-api/v0/connections/([0-9]+)/?$"             /profile-api/v0/me-connections.php?id=$1 last;
    rewrite "^/profile-api/v0/me/messages/?$"                      /profile-api/v0/me-messages.php last;
    rewrite "^/profile-api/v0/me/messages/([0-9a-f-]{36})/?$"     /profile-api/v0/me-thread.php?uuid=$1 last;
    rewrite "^/profile-api/v0/me/social-counts/?$"                /profile-api/v0/me-social-counts.php last;
    rewrite "^/profile-api/v0/me/notifications/?$"                /profile-api/v0/me-notifications.php last;
    rewrite "^/profile-api/v0/me/mutes/?$"                          /profile-api/v0/me-mutes.php last;
    rewrite "^/profile-api/v0/me/mutes/([0-9a-fA-F-]{36})/?$"       /profile-api/v0/me-mutes.php?uuid=$1 last;
    rewrite "^/profile-api/v0/me/preview/?$"                /profile-api/v0/me-preview.php     last;
    rewrite "^/profile-api/v0/me/claim/?$"                  /profile-api/v0/me-claim.php       last;
    rewrite "^/profile-api/v0/me/section-order/?$"          /profile-api/v0/me-section-order.php last;
    rewrite "^/profile-api/v0/me/name/?$"                   /profile-api/v0/me-name.php        last;
    rewrite "^/profile-api/v0/me/practices/?$"              /profile-api/v0/me-practices.php   last;
    rewrite "^/profile-api/v0/me/practices/([0-9a-fA-F-]{36})/?$"  /profile-api/v0/me-practices.php?uuid=$1 last;
    rewrite "^/profile-api/v0/practice/([0-9a-fA-F-]{36})/?$"  /profile-api/v0/practice.php?uuid=$1 last;
    rewrite "^/profile-api/v0/schema/?$"                    /profile-api/v0/schema.php         last;
    rewrite "^/profile-api/v0/catalogs/(skills|services|instruments|genres)/([0-9]+)/?$"  /profile-api/v0/catalogs.php?kind=$1&id=$2  last;
    rewrite "^/profile-api/v0/catalogs/(instruments|skills|scenes|credentials|services|genres)/?$"  /profile-api/v0/catalogs.php?kind=$1  last;
    rewrite "^/profile-api/v0/me/instruments/?$"            /profile-api/v0/me-instruments.php  last;
    rewrite "^/profile-api/v0/me/skills/?$"                 /profile-api/v0/me-skills.php       last;
    rewrite "^/profile-api/v0/me/scenes/?$"                 /profile-api/v0/me-scenes.php       last;
    rewrite "^/profile-api/v0/me/highlights/?$"             /profile-api/v0/me-highlights.php   last;
    rewrite "^/profile-api/v0/me/credentials/?$"            /profile-api/v0/me-credentials.php  last;
    rewrite "^/profile-api/v0/me/credentials/([0-9]+)/?$"   /profile-api/v0/me-credentials.php?id=$1  last;
    rewrite "^/profile-api/v0/reports/?$"                   /profile-api/v0/reports.php         last;
    rewrite "^/profile-api/v0/directory/members/?$"         /profile-api/v0/directory-members.php  last;
    rewrite "^/profile-api/v0/directory/pins-public/?$"     /profile-api/v0/directory-pins-public.php last;

    # Slice 3.5: strangler identity contract (internal/ is handled by an
    # outer ^~ block above for cookie-gate exemption + localhost lock).
    rewrite "^/profile-api/v0/whoami/?$"                    /profile-api/v0/whoami.php          last;
    rewrite "^/profile-api/v0/users/?$"                     /profile-api/v0/users.php           last;
    rewrite "^/profile-api/v0/sponsor/([a-z0-9-]{1,64})/?$"  /profile-api/v0/sponsor.php?slug=$1 last;
    rewrite "^/profile-api/v0/sponsor/?$"                    /profile-api/v0/sponsor.php         last;

location = /profile-api/v0/user-created.php {
        allow 127.0.0.1;
        allow ::1;
        deny all;
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    location = /profile-api/v0/internal-email-changed.php {
        allow 127.0.0.1;
        allow ::1;
        deny all;
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    # Schema is public — still behind the dev cookie gate (whole site is).
    location = /profile-api/v0/schema.php {
        if ($loothdev_is_authorized != 1) { return 403; }
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    # Public read (auth-aware via JWT cookie).
    location = /profile-api/v0/user.php {
        if ($loothdev_is_authorized != 1) { return 403; }
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    # Authed /me endpoints — JWT-gated in PHP; bypass dev cookie gate when
    # a Bearer token is present (PHP enforces real auth either way).
    location ~ "^/profile-api/v0/(me|me-header|me-about|me-location|me-socials|me-preview|me-claim|me-section-order|me-name|me-instruments|me-skills|me-scenes|me-highlights|me-credentials|me-location-search|me-practices|me-craft|me-gallery|me-connect|me-layout|me-catalog|me-lights|me-practice-header|me-practice-about|me-practice-layout|me-practice-block|me-practice-services|me-practice-instruments|me-avatar|me-connections|me-messages|me-thread|me-social-counts|me-notifications|me-mutes|me-banner|me-resume|me-freeform|me-dropoffs|me-discussion-visibility)\.php$" {
        set $profile_api_gate $loothdev_is_authorized;
        if ($http_authorization ~* "^Bearer ") { set $profile_api_gate 1; }
        if ($profile_api_gate != 1) { return 403; }
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    # Public/auth-aware endpoints.
    location ~ "^/profile-api/v0/(catalogs|directory-members|directory-pins-public|reports|practice|whoami|users|sponsor)\.php$" {
        if ($loothdev_is_authorized != 1) { return 403; }
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
    }

    location ~ \.php$ { return 403; }
}

# /directory/members SSR page
location = /directory/members {
    if ($loothdev_is_authorized != 1) { return 403; }
    include fastcgi.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
    fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/directory-members.php;
}
location ~ "^/directory/members/?$" {
    if ($loothdev_is_authorized != 1) { return 403; }
    include fastcgi.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm-profile-app.sock;
    fastcgi_param SCRIPT_FILENAME /srv/profile-app/web/directory-members.php;
}
