{
    "name": "loothgroup/billing-svc",
    "description": "Standalone entitlement/tier service — relocated poller (Arbiter + Patreon reader) and future tier authority for loothgroup.com. Holds NO Stripe keys at this stage; payment-credential handling stays in lg-stripe-billing.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.3",
        "ext-pdo": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "LGBilling\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "LGBilling\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
